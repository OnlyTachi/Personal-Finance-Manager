{
    # Configurações Globais ficam aqui, entre chaves no topo
    auto_https off
}

:80 {
    # Configurações do Site ficam aqui
    handle /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:5173
    }
}